from tkinter import *

root = Tk()  # creating the root object - window
root.title("zero-to-hero.dev")  # setting the title of the window
root.geometry("500x400")  # setting the size of the window

label = Label(text="Hello HERO!")
label.pack()  # place the label in the window
root.mainloop()
